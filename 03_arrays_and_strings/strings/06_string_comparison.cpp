#include <iostream>
#include <string>
using namespace std;

int main()
{
    string first = "AI";
    string second = "AI";

    if (first == second)
    {
        cout << "Strings are equal.";
    }
    else
    {
        cout << "Strings are different.";
    }

    return 0;
}